``{{event.type}}``
  Summary:
    {{event.summary}}
  Type:
    {{event.typeof}}
  Description:
    {{event.desc}}

  JSON Format::

    {{event.json_format | jsonify(4)}}

  Example::

    {{example.content | jsonify(4)}}
